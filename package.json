{
  "name": "v8-coverage-collector",
  "version": "0.1.2",
  "description": "",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "oxlint --quiet",
    "format": "prettier . --check"
  },
  "devDependencies": {
    "@prettier/plugin-oxc": "^0.1.3",
    "@types/node": "^24.0.0",
    "oxlint": "^1.43.0",
    "prettier": "^3.8.1",
    "typescript": "^6.0.0",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
